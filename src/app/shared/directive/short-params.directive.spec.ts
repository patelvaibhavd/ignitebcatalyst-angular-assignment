import { ShortParamsDirective } from './short-params.directive';

describe('ShortParamsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortParamsDirective();
    expect(directive).toBeTruthy();
  });
});
